diff --git a/resources/views/Layout/Partial/nav_auth.blade.php b/resources/views/Layout/Partial/nav_auth.blade.php
index 8f640af..e8fb8f5 100644
--- a/resources/views/Layout/Partial/nav_auth.blade.php
+++ b/resources/views/Layout/Partial/nav_auth.blade.php
@@ -1,7 +1,14 @@
-@auth
-    <li class="nav-item">
-        <a class="nav-link" href="{{ auth()->user()->isAdmin() ? route('admin.dashboard') : route('customer.orders.index') }}">{{ auth()->user()->isAdmin() ? 'Dashboard' : 'Pesanan Saya' }}</a>
+{{-- Menu akun di navbar publik: khusus pembeli. Admin masuk lewat /admin, tidak ditampilkan di sini. --}}
+@guest
+    <li class="nav-item ms-lg-2 mt-2 mt-lg-0">
+        <a class="btn btn-sm px-4" href="{{ route('login') }}"
+           style="--bs-btn-color:#fff; --bs-btn-bg:#2A6CA2; --bs-btn-border-color:#2A6CA2; --bs-btn-hover-color:#fff; --bs-btn-hover-bg:#1e4e78; --bs-btn-hover-border-color:#1e4e78; --bs-btn-active-color:#fff; --bs-btn-active-bg:#1e4e78; --bs-btn-active-border-color:#1e4e78; border-radius:50px; font-weight:600; font-size:14px; padding-top:7px; padding-bottom:7px;">Masuk</a>
     </li>
-@else
-    <li class="nav-item"><a class="nav-link" href="{{ route('login') }}">Masuk</a></li>
+@endguest
+@auth
+    @unless(auth()->user()->isAdmin())
+        <li class="nav-item">
+            <a class="nav-link {{ request()->routeIs('customer.orders.*') ? 'active' : '' }}" href="{{ route('customer.orders.index') }}">Pesanan Saya</a>
+        </li>
+    @endunless
 @endauth
diff --git a/routes/web.php b/routes/web.php
index 076bd65..8d479be 100644
--- a/routes/web.php
+++ b/routes/web.php
@@ -51,6 +51,9 @@
 // 3. Route Admin Panel (DIKUNCI: harus login DAN berperan admin)
 // Pelanggan yang sudah login tetap tidak bisa masuk ke route di dalam kotak ini
 Route::middleware(['auth', 'admin'])->group(function () {
+
+    // Pintu masuk admin: ketik /admin di browser (tidak ada tautannya di navbar publik)
+    Route::redirect('/admin', '/admin/dashboard');
     
     // Dashboard
     Route::get('/admin/dashboard', function () {
diff --git a/tests/Feature/PublicPagesTest.php b/tests/Feature/PublicPagesTest.php
index 0bcca4e..9b8597d 100644
--- a/tests/Feature/PublicPagesTest.php
+++ b/tests/Feature/PublicPagesTest.php
@@ -20,17 +20,39 @@ public function test_public_pages_show_login_link_to_guests(): void
         }
     }
 
-    public function test_public_pages_show_orders_link_to_customers_and_dashboard_to_admins(): void
+    public function test_customer_sees_orders_link_and_no_login_button(): void
     {
         $customer = User::factory()->create();
+
+        foreach (self::PAGES as $url) {
+            $this->actingAs($customer)->get($url)->assertOk()
+                ->assertSee('Pesanan Saya')
+                ->assertDontSee('>Masuk<', false);
+        }
+    }
+
+    public function test_navbar_never_links_to_admin_dashboard(): void
+    {
         $admin = User::factory()->admin()->create();
+        $customer = User::factory()->create();
 
         foreach (self::PAGES as $url) {
-            $this->actingAs($customer)->get($url)->assertOk()->assertSee('Pesanan Saya');
-            $this->actingAs($admin)->get($url)->assertOk()->assertSee('Dashboard');
+            $this->get($url)->assertOk()->assertDontSee('/admin');
+            $this->actingAs($customer)->get($url)->assertOk()->assertDontSee('/admin');
+            $this->actingAs($admin)->get($url)->assertOk()
+                ->assertDontSee('/admin')
+                ->assertDontSee('Pesanan Saya')
+                ->assertDontSee('>Masuk<', false);
         }
     }
 
+    public function test_admin_entry_point_is_slash_admin(): void
+    {
+        $this->get('/admin')->assertRedirect('/login');
+        $this->actingAs(User::factory()->create())->get('/admin')->assertForbidden();
+        $this->actingAs(User::factory()->admin()->create())->get('/admin')->assertRedirect('/admin/dashboard');
+    }
+
     public function test_product_button_goes_to_order_form_with_reference(): void
     {
         $product = Product::create(['image' => 'x.jpg', 'name' => 'Pin Enamel', 'price' => 10000, 'description' => 'tes']);
